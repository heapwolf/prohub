var jade = require('jade');

var templateFile = __dirname + '/templates/index.jade';
var options = { doctype: 'html5' };
var templateFunction = jade.compileFile(templateFile, options);

//
// TODO
// Pull in the config to set up specifics for running the app.
//

module.exports = templateFunction;

