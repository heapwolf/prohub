module.exports = require('rc')('prohub', {

  local: {
    port: 8080,
    host: "0.0.0.0"
  },

  production: {
    port: 80,
    host: "0.0.0.0"
  },
  
  // Organization name
  org: '',

  // GitHub client ID
  githubClient: '',

  // GitHub client secret
  githubSecret: '',

  // Application Homepage URL
  baseURL: 'http://localhost:8080'
});

