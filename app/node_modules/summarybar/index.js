var jade = require('jade');
var templateFile = __dirname + '/templates/index.jade';
module.exports = jade.compileFile(templateFile);

/* module.exports = function createSummaryBar() {

  var sb = document.querySelector('.summarybar');
  var sbRepos = sb.querySelector('.repos');
  var sbIssues = sb.querySelector('.issues');
  var sbMilestones = sb.querySelector('.milestones');
  var sbContributors = sb.querySelector('.contributors');
  var sbContributions = sb.querySelector('.contributions');

  return function fill(data) {
    sbIssues.innerText = data.issues;
    sbMilestones.innerText = data.milestones;
    sbContributors.innerText = data.contributors;
    sbContributions.innerText = data.contributions;
    sbRepos.innerText = data.repos;
  }
};
*/

